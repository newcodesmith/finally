---
phase: 01-foundation-market-data-engine
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: []
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
  - MKT-01
  - MKT-05
  - INFRA-05

must_haves:
  truths:
    - "FastAPI server starts without errors using uvicorn"
    - "GET /api/health returns JSON with status ok and database connected"
    - "SQLite database file is auto-created with all 6 tables on first start"
    - "Database is seeded with default user ($10k cash) and 10 watchlist tickers"
    - "GET /api/stream/prices returns SSE events with all required fields"
    - "SSE events contain ticker, price, previous_price, session_open_price, timestamp, change_direction"
  artifacts:
    - path: "db/finally.db"
      provides: "Auto-created SQLite database"
    - path: "backend/app/main.py"
      provides: "FastAPI app with lifespan managing market data"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/db/schema.py"
      via: "init_db() in lifespan"
      pattern: "await init_db"
    - from: "backend/app/main.py"
      to: "backend/app/market/stream.py"
      via: "create_stream_router"
      pattern: "create_stream_router"
    - from: "backend/app/market/stream.py"
      to: "backend/app/market/cache.py"
      via: "PriceCache reads in SSE loop"
      pattern: "price_cache"
---

<objective>
Integration verification: Start the FastAPI server, confirm the database initializes with seed data, verify the health endpoint checks DB connectivity, and confirm SSE price streaming works end-to-end.

Purpose: Unit tests proved individual components work. This plan verifies the full system integration — that the server starts, the database initializes, the market simulator feeds the price cache, and SSE streaming delivers price events to clients.

Output: Verified working server with SSE streaming confirmed via curl.
</objective>

<execution_context>
@/Users/michaelsmith/Documents/Personal/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelsmith/Documents/Personal/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-market-data-engine/01-CONTEXT.md
@.planning/phases/01-foundation-market-data-engine/01-01-SUMMARY.md

@backend/app/main.py
@backend/app/api/health.py
@backend/app/db/schema.py
@backend/app/market/stream.py

<interfaces>
From backend/app/main.py:
```python
# FastAPI app with lifespan that:
# 1. Calls init_db() to create/seed database
# 2. Starts market_source with watchlist tickers
# 3. Registers routes: health, watchlist, portfolio, chat, stream
# Run with: uvicorn app.main:app --host 0.0.0.0 --port 8000
```

SSE event format (from market/stream.py):
```
data: {"ticker": "AAPL", "price": 190.50, "previous_price": 190.30, "session_open_price": 189.00, "timestamp": "2024-01-01T12:00:00Z", "change_direction": "up"}
```

Health response format (from api/health.py after Plan 01):
```json
{"status": "ok", "database": "connected", "users": 1}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start server and verify database initialization + health + SSE streaming</name>
  <files></files>
  <read_first>
    - backend/app/main.py
    - backend/app/db/schema.py
    - backend/app/market/stream.py
    - backend/app/api/health.py
  </read_first>
  <action>
1. Remove any stale database files to test fresh initialization:
   `rm -f /Users/michaelsmith/Documents/Personal/finally/db/finally.db /Users/michaelsmith/Documents/Personal/finally/db/finally_test.db /Users/michaelsmith/Documents/Personal/finally/db/finally_test2.db`

2. Start the FastAPI server in the background:
   `cd /Users/michaelsmith/Documents/Personal/finally/backend && uv run uvicorn app.main:app --host 127.0.0.1 --port 8000 &`
   Wait 3 seconds for startup.

3. Verify the health endpoint with database connectivity:
   `curl -s http://127.0.0.1:8000/api/health`
   Expected: `{"status":"ok","database":"connected","users":1}`
   This confirms DB-01 (lazy init), DB-02 (tables created), DB-03 (seed data — 1 user), INFRA-05 (health endpoint).

4. Verify the database was created and has all 6 tables:
   `sqlite3 /Users/michaelsmith/Documents/Personal/finally/db/finally.db ".tables"`
   Expected output should include: chat_messages, portfolio_snapshots, positions, trades, users_profile, watchlist

5. Verify seed data — 10 watchlist tickers and $10k cash:
   `sqlite3 /Users/michaelsmith/Documents/Personal/finally/db/finally.db "SELECT COUNT(*) FROM watchlist; SELECT cash_balance FROM users_profile WHERE id='default';"`
   Expected: 10 tickers, 10000.0 cash balance

6. Verify SSE streaming works — capture 3 seconds of price events:
   `timeout 3 curl -s http://127.0.0.1:8000/api/stream/prices 2>/dev/null || true`
   Verify the output contains SSE `data:` lines with JSON objects containing all required fields:
   - ticker (string)
   - price (number)
   - previous_price (number)
   - session_open_price (number)
   - timestamp (ISO string)
   - change_direction ("up", "down", or "unchanged")
   This confirms MKT-01 (SSE streaming) and MKT-05 (price cache feeding SSE).

7. Stop the server:
   `kill %1 2>/dev/null || pkill -f "uvicorn app.main:app" || true`

8. Clean up the test database:
   `rm -f /Users/michaelsmith/Documents/Personal/finally/db/finally.db`

If any step fails, diagnose the issue. Common problems:
- Port 8000 already in use: `lsof -i :8000` to find and kill the process
- Import errors: check that `uv sync` completed successfully in Plan 01
- Database path issues: the app writes to `db/finally.db` relative to the backend parent directory
  </action>
  <verify>
    <automated>cd /Users/michaelsmith/Documents/Personal/finally/backend && rm -f ../db/finally.db && (uv run uvicorn app.main:app --host 127.0.0.1 --port 8321 &) && sleep 3 && curl -sf http://127.0.0.1:8321/api/health && echo "" && timeout 3 curl -s http://127.0.0.1:8321/api/stream/prices 2>/dev/null | head -5; kill %1 2>/dev/null; rm -f ../db/finally.db</automated>
  </verify>
  <acceptance_criteria>
    - FastAPI server starts without import or configuration errors
    - `curl /api/health` returns JSON containing `"status":"ok"` and `"database":"connected"`
    - `sqlite3 db/finally.db ".tables"` shows all 6 tables: chat_messages, portfolio_snapshots, positions, trades, users_profile, watchlist
    - Watchlist has 10 seed entries, users_profile has cash_balance of 10000.0
    - `curl /api/stream/prices` returns SSE data lines containing JSON with keys: ticker, price, previous_price, session_open_price, timestamp, change_direction
    - SSE events arrive within 3 seconds of connecting (confirming ~500ms cadence)
  </acceptance_criteria>
  <done>FastAPI server starts cleanly, database auto-initializes with all 6 tables and seed data, health endpoint confirms DB connectivity, and SSE streaming delivers price events with all required fields. All Phase 1 success criteria from ROADMAP verified end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curl -> FastAPI | Local testing only, no external exposure |
| FastAPI -> SQLite | Backend writes to local filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T (Tampering) | db/finally.db | accept | Local development database with no real user data. Volume-mounted in Docker for production isolation. |
| T-01-06 | D (Denial of Service) | uvicorn on port 8000 | accept | Development server, local access only. Not exposed to internet. |
</threat_model>

<verification>
1. Server starts: `uvicorn app.main:app` exits cleanly after lifespan initialization
2. Health check: `curl /api/health` returns `{"status":"ok","database":"connected",...}`
3. Database: `sqlite3 db/finally.db ".tables"` returns 6 tables
4. Seed data: 10 watchlist entries, $10k cash balance
5. SSE: `curl /api/stream/prices` returns `data:` events with required JSON fields
</verification>

<success_criteria>
- FastAPI server starts and serves API endpoints on port 8000
- Database auto-created with all 6 tables (DB-01, DB-02)
- Seed data present: default user with $10k, 10 watchlist tickers (DB-03)
- Health endpoint returns database connectivity status (INFRA-05)
- SSE stream delivers price events with ticker, price, previous_price, session_open_price, timestamp, change_direction (MKT-01, MKT-05)
- All 5 ROADMAP Phase 1 success criteria confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-market-data-engine/01-02-SUMMARY.md`
</output>
